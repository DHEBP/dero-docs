const config = {
  siteUrl: 'https://derod.org', // keep in sync with seo.config.ts
  generateRobotsTxt: true,
  sitemapSize: 7000,
  changefreq: 'weekly',
  priority: 0.7,
  trailingSlash: false
}

export default config; 